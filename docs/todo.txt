Background

Unit Sizing
    - Define Entity size and use SpriteBundle.sprite.custom_size to specify this on load
    - this could also be used for initialising boxcolliders

Event system
    - rather than check every frame for certain conditions, can clean up with events
    - player_damage event to check for death, update healthbar, update health   
    - kill_event to update scoreboard

Blood
    - hostiles drop blood when killed, temporary
    - blood skating